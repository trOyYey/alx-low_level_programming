#include "main.h"

/**
 * times_table - print the 9 times table, starting with 0
 */

void times_table(void);
{
	int a, b, result;

	for (a = 0 ; i < 10; a++)
	{
		for (b = 0; b < 10; b++)
		{
			result = a * b
			if (b == 0)
				printf("%d, ", result);
			else
			{
				printf("%2d", result);
				if (j != 9)
					printf(", ");
			}
		}
		printf("\n");
	}
}
